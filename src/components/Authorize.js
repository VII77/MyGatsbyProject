import React, {useEffect} from "react"
import * as admin from "firebase-admin"

export const Authorize = () => {

    useEffect(() => {
        admin.initialzeApp();
    }, [])

    exports.add

  return <div></div>
}
